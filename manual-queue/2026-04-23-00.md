# Engagement Manual Queue — 2026-04-23 00:46 UTC

**Status:** Infrastructure setup complete. Awaiting comment data.

---

## Run Summary

| Platform | Auto-replies | Manual Queue | Notes |
|----------|-------------|--------------|-------|
| X/Twitter | 0 | 0 | Ready to respond; waiting for post URLs from comment sources |
| Facebook | 0 | 0 | PC asleep; manual queue prepared if items arrive |
| Instagram | 0 | 0 | PC asleep; manual queue prepared if items arrive |
| LinkedIn | 0 | 0 | PC asleep; manual queue prepared if items arrive |

---

## Infrastructure Notes

✅ **Engagement logging system:** Active at `./generated/engagement-log.jsonl`
✅ **State management:** Updated `rotation-state.json` with run timestamp
✅ **Blotato API:** Connected to X/Twitter account (aiwithedison)
⏸ **Comment data sources:** Not yet wired (see below)

---

## Data Source Configuration

### To Enable X/Twitter Automation
The routine is **ready to send replies** but needs access to comment data. Options:

1. **Option A: Direct comment URLs** — If you use a post scheduler, tweet management tool, or mobile app, copy/paste recent post URLs here for processing.
2. **Option B: Post feed API** — Enable Edison's post history export (if available from scheduler or X API).
3. **Option C: Manual input** — During testing, paste sample comment JSON for classification.

Once enabled, the routine will:
- Classify each comment (resource_request | question | compliment | negative | unclear)
- Generate 25-word replies in Edison's voice
- Post via Blotato automatically (up to 20/run)

### To Enable Facebook / Instagram / LinkedIn
Keep your PC running during scheduled engagement times (hourly by default).
- When PC is awake: routine auto-replies via Claude-in-Chrome (your real browser)
- When PC is asleep: replies are prepared here as manual DM copy-paste packages

---

## PDF Resource Links

Currently populated resource map for engaged comments:

```json
{
  "x_pdf_links": {},
  "facebook_pdf_links": {},
  "instagram_pdf_links": {},
  "linkedin_pdf_links": {}
}
```

When a commenter asks for a guide and no link exists yet, the routine will:
1. Auto-generate a branded PDF (navy #0A1628 + yellow #FFD700)
2. Upload to Drive folder `1MyvXqCm8Mhs02OCX1qyWotsT3Pj37Sm-`
3. Save link to `rotation-state.json`
4. Include in replies

To pre-populate a resource link:
- Edit `rotation-state.json`
- Add entry: `"x_pdf_links": { "topic_slug": "https://drive.google.com/..." }`
- Commit and push

---

## Next Hourly Run (2026-04-23 01:46 UTC)

The routine will check again at the top of the next hour. If still no comment data:
- Same infrastructure check
- State updated with fresh timestamp
- Ready to process when comments arrive

---

**Questions?** Refer to `comment-engagement-responder/SKILL.md` for full automation rules and voice guidelines.
